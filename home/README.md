> [!NOTE]
> The .bashrc file is a hidden configuration file located in your user home directory (~/.bashrc).

To install and use the included .bashrc config, copy it directly into your home directory.

If you already have an existing config, make sure to back it up first or copy over any custom aliases and environment variables you need. Once copied, apply changes immediately by running:

```bash
source ~/.bashrc
```
